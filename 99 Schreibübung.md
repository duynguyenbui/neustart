# Schreibübung — Luyện Viết Tiếng Đức (Mục tiêu: B1)

> [!IMPORTANT]
> Mỗi dòng văn có giải thích ngữ pháp & nghĩa ngay bên dưới.
> Ký hiệu: `từ` = **[loại từ]** — *nghĩa* — *(ghi chú ngữ pháp)*

---

## Cấu Trúc Bài Thi Schreiben B1 (Goethe-Zertifikat)

| Phần | Dạng bài | Số từ | Thời gian |
|------|---------|-------|----------|
| **Teil 1** | Thư / Email bán trang trọng | ~80 từ | ~20 phút |
| **Teil 2** | Bình luận / Nêu ý kiến về chủ đề | ~80 từ | ~20 phút |

---

## Bài 01 — Email Xin Lỗi & Thay Đổi Lịch Hẹn (Teil 1)

**Đề bài:** Ihr Freund Max hat Sie zu seiner Geburtstagsfeier eingeladen. Sie können leider nicht kommen, weil Sie krank sind. Schreiben Sie Max eine E-Mail: Entschuldigen Sie sich, erklären Sie Ihren Grund und machen Sie einen neuen Termin.

---

Lieber Max,
> `Lieber` = **Tính từ** — *thân mến* — dùng với danh từ giống đực (`Max` là nam) → đuôi `-er`; nếu là nữ: `Liebe Anna`

leider kann ich nicht zu deiner Geburtstagsfeier kommen, weil ich krank bin.
> `leider` = **Trạng từ** — *tiếc là* — đứng đầu câu → Inversion: động từ `kann` lên trước chủ ngữ `ich`
> `kann` = **Động từ khuyết thiếu** — *có thể* — `können` chia ngôi 1 số ít; động từ chính `kommen` bị đẩy ra cuối
> `zu deiner Geburtstagsfeier` = **Cụm giới từ** — *đến bữa tiệc sinh nhật của bạn* — `zu` + Dativ: `deiner` (sở hữu của `du`, giống cái Dativ)
> `weil ich krank bin` = **Mệnh đề phụ** — *vì tôi bị ốm* — `weil` → động từ `bin` ra cuối

Es tut mir wirklich leid!
> `Es tut mir leid` = **Cụm cố định** — *Tôi xin lỗi / Tôi rất tiếc* — `mir` là Dativ của `ich`; `wirklich` = *thật sự* để nhấn mạnh

Ich habe seit drei Tagen Fieber und muss zu Hause bleiben.
> `seit drei Tagen` = **Cụm giới từ** — *từ ba ngày nay* — `seit` + Dativ: dùng khi hành động bắt đầu trong quá khứ và **vẫn đang tiếp diễn**
> `Fieber` = **Danh từ** — *sốt* — `das Fieber`; không dùng mạo từ khi nói chung
> `muss zu Hause bleiben` = **Động từ** — *phải ở nhà* — `müssen` (phải, do hoàn cảnh) + `zu Hause` (ở nhà, trạng thái) ≠ `nach Hause` (về nhà, di chuyển)

Der Arzt hat mir gesagt, dass ich mich ausruhen soll.
> `hat gesagt` = **Perfekt** — *đã nói* — `sagen` → Perfekt: `hat + gesagt`
> `mir` = **Dativ** — *cho tôi* — ai được nói tới
> `dass ich mich ausruhen soll` = **Mệnh đề phụ** — `dass` → `soll` ra cuối
> `mich ausruhen` = **Động từ phản thân** — *nghỉ ngơi* — `sich ausruhen`; chủ ngữ `ich` → dùng `mich`
> `soll` = **Động từ khuyết thiếu** — *nên/phải* — `sollen` = lời yêu cầu từ **người khác** (bác sĩ nói) ≠ `müssen` (tự thấy cần)

Können wir uns nächste Woche treffen?
> `Können` đứng đầu = **câu hỏi Yes/No** — *Chúng ta có thể... không?*
> `uns treffen` = **Động từ phản thân** — *gặp nhau* — `sich treffen`; ngôi `wir` → `uns`
> `nächste Woche` = **Cụm danh từ** — *tuần tới* — không có mạo từ → tính từ chia mạnh (stark): `-e`

Ich würde dich gerne zum Essen einladen, als kleine Entschuldigung.
> `würde ... einladen` = **Konjunktiv II** — *tôi muốn mời* — `würde` + Infinitiv = lịch sự hơn `ich will`
> `dich` = **Akkusativ** — *bạn* — tân ngữ trực tiếp của `du`
> `gerne` = **Trạng từ** — *rất vui lòng, muốn* — diễn đạt mong muốn lịch sự
> `zum Essen` = **Cụm giới từ** — *đi ăn* — `zu` + `dem` = `zum`; `Essen` là danh từ hóa từ động từ `essen`
> `einladen` = **Động từ tách** — *mời* — tiền tố `ein-` tách ra cuối câu
> `als kleine Entschuldigung` = *như một lời xin lỗi nhỏ* — `als` = như là; `kleine` chia yếu (schwach) sau mạo từ không xác định

Herzliche Grüße,
> `Herzliche Grüße` = **Cụm cố định** — *Lời chào thân ái* — kết thư thân mật; trang trọng hơn dùng `Mit freundlichen Grüßen`

---

## Bài 02 — Nêu Ý Kiến Về Học Online (Teil 2)

**Đề bài:** Immer mehr Menschen lernen online. Was denken Sie darüber? Nennen Sie Vorteile und Nachteile.

---

Meiner Meinung nach hat das Online-Lernen sowohl Vorteile als auch Nachteile.
> `Meiner Meinung nach` = **Cụm cố định** — *theo ý kiến của tôi* — `Meinung` ở Dativ; `nach` đứng sau → Inversion: `hat` lên trước `das Online-Lernen`
> `das Online-Lernen` = **Danh từ hóa** — *việc học trực tuyến* — danh từ hóa từ cụm `online lernen` → giống trung (das)
> `sowohl ... als auch` = **Liên từ tương quan** — *vừa... vừa / cả... lẫn* — nối hai thành phần đồng vai: `sowohl A als auch B`

Einerseits ist es sehr flexibel, weil man von überall lernen kann.
> `Einerseits` = **Trạng từ** — *một mặt* — luôn dùng cặp với `andererseits` *(mặt khác)* → Inversion: `ist` lên trước `es`
> `man` = **Đại từ bất định** — *người ta, mình* — dùng khi chủ thể chung chung, không xác định
> `weil man ... lernen kann` = **Mệnh đề phụ** — `weil` → `kann` ra cuối

Man spart auch Zeit und Geld, da man nicht pendeln muss.
> `spart` = **Động từ** — *tiết kiệm* — `sparen`, ngôi 3 / `man`
> `Zeit und Geld` = **Tân ngữ Akkusativ** — *thời gian và tiền bạc*
> `da` = **Liên từ** — *vì* — tương đương `weil` nhưng dùng khi lý do đã ngầm hiểu; thường đứng đầu câu
> `pendeln` = **Động từ** — *đi lại hàng ngày (giữa nhà và nơi làm/học)*

Andererseits ist es schwieriger, sich zu konzentrieren.
> `Andererseits` = **Trạng từ** — *mặt khác* — cặp với `einerseits` → Inversion: `ist` lên trước `es`
> `schwieriger` = **Tính từ so sánh hơn** — *khó hơn* — `schwierig` + `-er`
> `sich zu konzentrieren` = **Cụm Infinitiv** — *tập trung* — `sich konzentrieren` (phản thân) + `zu`

Außerdem fehlt der persönliche Kontakt zu Lehrern und Mitschülern.
> `Außerdem` = **Trạng từ** — *ngoài ra, hơn nữa* — bổ sung thêm luận điểm → Inversion: `fehlt` lên trước `der Kontakt`
> `fehlt` = **Động từ** — *thiếu* — `fehlen` ngôi 3; chủ ngữ `der Kontakt` (giống đực, Nominativ)
> `zu Lehrern und Mitschülern` = **Cụm giới từ** — *với giáo viên và bạn học* — `zu` + Dativ số nhiều (không mạo từ)

Insgesamt denke ich, dass Online-Lernen eine gute Ergänzung zum traditionellen Unterricht ist, aber kein vollständiger Ersatz sein kann.
> `Insgesamt` = **Trạng từ** — *nhìn chung* — mở đầu kết luận → Inversion: `denke` lên trước `ich`
> `dass ... ist` = **Mệnh đề phụ** — `dass` → `ist` và `kann` ra cuối
> `eine gute Ergänzung` = *một sự bổ sung tốt* — `die Ergänzung`; `gute` chia yếu (schwach) sau `eine`
> `zum traditionellen Unterricht` = `zu` + `dem` = `zum`; `traditionellen` chia yếu sau `dem`
> `kein vollständiger Ersatz` = *không phải sự thay thế hoàn toàn* — `kein` phủ định danh từ; `vollständiger` chia mạnh (stark) vì không có mạo từ
